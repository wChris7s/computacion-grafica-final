#ifndef GLFW_COLORESENUM_H
#define GLFW_COLORESENUM_H

#include "ColorUtil.h"
#include "glm/vec3.hpp"

namespace colors {
    const glm::vec3 ROJO(1.0f, 0.0f, 0.0f);
    const glm::vec3 VERDE(1.0f, 0.0f, 0.0f);
    const glm::vec3 AZUL(1.0f, 0.0f, 0.0f);
    const glm::vec3 AMARILLO(1.0f, 0.0f, 0.0f);
    const glm::vec3 BLANCO(1.0f, 0.0f, 0.0f);
    const glm::vec3 NARANJA(1.0f, 0.0f, 0.0f);
    const glm::vec3 NEGRO(0.0f, 0.0f, 0.0f);
}

#endif //GLFW_COLORESENUM_H
